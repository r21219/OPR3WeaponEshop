import { UpdateCartRequest } from './update-cart-request';

describe('UpdateCartRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateCartRequest()).toBeTruthy();
  });
});
